name: pr-description
description: >
  Generate a PR Description using `git diff main`
parameters:
  check for the all files changes using git diff main and generate single or two line PR Description which can be used in PRs.